<% if user_signed_in? %>
<span>Здравствуйте, <%= current_user.email %></span>
    <%= image_tag current_user.avatar.url(:thumb), class: 'rounded-circle', style: 'height: 100px' if current_user.avatar? %>
<%= link_to 'Change details', edit_user_registration_path %>
<%= button_to 'Выйти', destroy_user_session_path, method: :delete %>
<% else %>
<%= link_to 'Войти', new_user_session_path %> или <%= link_to 'Зарегистрироваться', new_user_registration_path %>
<% end %>